<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .order-details-container {
            max-width: 900px;
            margin: 40px auto;
            padding: 30px;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        
        .section-title {
            color: #2c3e50;
            border-bottom: 2px solid #f0f0f0;
            padding-bottom: 10px;
            margin-top: 30px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .section-title i {
            color: #3498db;
        }
        
        .order-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
        }
        
        .order-id {
            font-size: 1.2rem;
            color: #7f8c8d;
        }
        
        .order-status {
            padding: 8px 15px;
            border-radius: 20px;
            font-weight: bold;
            display: inline-block;
            text-align: center;
        }
        
        .status-pending {
            background-color: #f39c12;
            color: white;
        }
        
        .status-placed {
            background-color: #3498db;
            color: white;
        }
        
        .status-shipped {
            background-color: #9b59b6;
            color: white;
        }
        
        .status-delivered {
            background-color: #2ecc71;
            color: white;
        }
        
        .status-cancelled {
            background-color: #e74c3c;
            color: white;
        }
        
        .payment-status {
            padding: 5px 12px;
            border-radius: 15px;
            font-size: 0.9rem;
            font-weight: bold;
            display: inline-block;
        }
        
        .payment-paid {
            background-color: #2ecc71;
            color: white;
        }
        
        .payment-pending {
            background-color: #f39c12;
            color: white;
        }
        
        .info-card {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
        }
        
        .info-row {
            display: flex;
            margin-bottom: 10px;
        }
        
        .info-label {
            width: 150px;
            font-weight: bold;
            color: #555;
        }
        
        .info-value {
            flex-grow: 1;
        }
        
        .product-card {
            display: flex;
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
        }
        
        .product-image {
            width: 120px;
            height: 120px;
            object-fit: cover;
            border-radius: 8px;
            margin-right: 20px;
        }
        
        .product-details {
            flex-grow: 1;
        }
        
        .product-title {
            font-size: 1.2rem;
            font-weight: bold;
            margin-bottom: 10px;
        }
        
        .product-price {
            font-size: 1.2rem;
            color: #27ae60;
            font-weight: bold;
            margin-bottom: 10px;
        }
        
        .product-category {
            color: #7f8c8d;
            margin-bottom: 5px;
        }
        
        .product-seller {
            color: #7f8c8d;
        }
        
        .summary-card {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
        }
        
        .summary-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }
        
        .summary-total {
            font-size: 1.2rem;
            font-weight: bold;
            margin-top: 10px;
            padding-top: 10px;
            border-top: 1px solid #ddd;
        }
        
        .action-buttons {
            display: flex;
            justify-content: space-between;
            margin-top: 30px;
            gap: 15px;
        }
        
        .btn {
            padding: 10px 20px;
            border-radius: 5px;
            border: none;
            cursor: pointer;
            font-weight: bold;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s;
            text-align: center;
        }
        
        .btn-primary {
            background-color: #3498db;
            color: white;
        }
        
        .btn-primary:hover {
            background-color: #2980b9;
        }
        
        .btn-danger {
            background-color: #e74c3c;
            color: white;
        }
        
        .btn-danger:hover {
            background-color: #c0392b;
        }
        
        .btn-success {
            background-color: #2ecc71;
            color: white;
        }
        
        .btn-success:hover {
            background-color: #27ae60;
        }
        
        .timeline {
            position: relative;
            margin: 30px 0;
            padding-left: 30px;
        }
        
        .timeline::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 2px;
            background-color: #ddd;
        }
        
        .timeline-item {
            position: relative;
            margin-bottom: 20px;
            padding-bottom: 20px;
        }
        
        .timeline-item:last-child {
            margin-bottom: 0;
            padding-bottom: 0;
        }
        
        .timeline-item::before {
            content: '';
            position: absolute;
            left: -38px;
            top: 0;
            width: 14px;
            height: 14px;
            border-radius: 50%;
            background-color: #3498db;
        }
        
        .timeline-date {
            color: #7f8c8d;
            font-size: 0.9rem;
            margin-bottom: 5px;
        }
        
        .timeline-event {
            font-weight: bold;
        }
        
        .cancel-form {
            display: inline-block;
        }
        
        @media (max-width: 768px) {
            .order-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 10px;
            }
            
            .info-row {
                flex-direction: column;
                margin-bottom: 15px;
            }
            
            .product-card {
                flex-direction: column;
            }
            
            .product-image {
                width: 100%;
                margin-right: 0;
                margin-bottom: 15px;
            }
            
            .action-buttons {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>

<%- include('partials/header') %>

<div class="container">
    <div class="order-details-container">
        <div class="order-header">
            <h1>Order Details</h1>
            <div class="order-id">Order #<%= order._id %></div>
        </div>
        
        <div class="info-card">
            <div class="info-row">
                <div class="info-label">Order Status:</div>
                <div class="info-value">
                    <span class="order-status status-<%= order.orderStatus ? order.orderStatus.toLowerCase() : 'unknown' %>"><%= order.orderStatus ? (order.orderStatus.charAt(0).toUpperCase() + order.orderStatus.slice(1)) : 'Unknown' %></span>
                </div>
            </div>
            <div class="info-row">
                <div class="info-label">Payment Status:</div>
                <div class="info-value">
                    <span class="payment-status <%= order.isPaid ? 'payment-paid' : 'payment-pending' %>">
                        <%= order.isPaid ? 'Paid' : 'Pending' %>
                    </span>
                </div>
            </div>
            <div class="info-row">
                <div class="info-label">Order Date:</div>
                <div class="info-value"><%= order.createdAt || 'Not available' %></div>
            </div>
            <% if (order.isPaid && order.paidAt) { %>
                <div class="info-row">
                    <div class="info-label">Payment Date:</div>
                    <div class="info-value"><%= order.paidAt %></div>
                </div>
            <% } %>
        </div>
        
        <h2 class="section-title"><i class="fas fa-box"></i> Item Details</h2>
        <% if (order.listing) { %>
            <div class="product-card">
                <img src="<%= order.listing.images && order.listing.images.length > 0 ? order.listing.images[0] : '/images/default-product.png' %>" 
                     alt="<%= order.listing.title || 'Product' %>" class="product-image">
                <div class="product-details">
                    <div class="product-title"><%= order.listing.title || 'Product title not available' %></div>
                    <div class="product-price">$<%= order.listing.price || 'Price not available' %></div>
                    <div class="product-category"><i class="fas fa-tag"></i> <%= order.listing.category || 'Category not available' %></div>
                    <div class="product-seller"><i class="fas fa-user"></i> Seller: <%= order.seller && order.seller.fullname ? order.seller.fullname : 'Unknown seller' %></div>
                    <div class="product-seller"><i class="fas fa-envelope"></i> <%= order.seller && order.seller.email ? order.seller.email : 'Email not available' %></div>
                </div>
            </div>
        <% } else { %>
            <div class="info-card">
                <p style="text-align: center; color: #e74c3c;">
                    <i class="fas fa-exclamation-triangle"></i> Product information is not available for this order.
                </p>
            </div>
        <% } %>
        
        <h2 class="section-title"><i class="fas fa-truck"></i> Shipping Information</h2>
        <% if (order.shippingInfo) { %>
            <div class="info-card">
                <div class="info-row">
                    <div class="info-label">Name:</div>
                    <div class="info-value"><%= order.shippingInfo.name || 'Not provided' %></div>
                </div>
                <div class="info-row">
                    <div class="info-label">Address:</div>
                    <div class="info-value"><%= order.shippingInfo.address || 'Not provided' %></div>
                </div>
                <div class="info-row">
                    <div class="info-label">City:</div>
                    <div class="info-value"><%= order.shippingInfo.city || 'Not provided' %></div>
                </div>
                <div class="info-row">
                    <div class="info-label">Pincode:</div>
                    <div class="info-value"><%= order.shippingInfo.pincode || 'Not provided' %></div>
                </div>
                <div class="info-row">
                    <div class="info-label">Phone:</div>
                    <div class="info-value"><%= order.shippingInfo.phone || 'Not provided' %></div>
                </div>
            </div>
        <% } else { %>
            <div class="info-card">
                <p style="text-align: center; color: #e74c3c;">
                    <i class="fas fa-exclamation-triangle"></i> Shipping information is not available for this order.
                </p>
            </div>
        <% } %>
        
        <h2 class="section-title"><i class="fas fa-file-invoice-dollar"></i> Payment Information</h2>
        <% if (order.paymentInfo) { %>
            <div class="info-card">
                <div class="info-row">
                    <div class="info-label">Amount:</div>
                    <div class="info-value">₹<%= order.paymentInfo.amount || 'Not available' %></div>
                </div>
                <div class="info-row">
                    <div class="info-label">Payment Method:</div>
                    <div class="info-value">
                        <% if (order.paymentInfo.method) { %>
                            <i class="fab fa-<%= order.paymentInfo.method %>"></i> 
                            <%= order.paymentInfo.method.charAt(0).toUpperCase() + order.paymentInfo.method.slice(1) %>
                        <% } else { %>
                            Not specified
                        <% } %>
                    </div>
                </div>
                <% if (order.paypalOrderId) { %>
                    <div class="info-row">
                        <div class="info-label">Transaction ID:</div>
                        <div class="info-value"><%= order.paypalOrderId %></div>
                    </div>
                <% } %>
            </div>
        <% } else { %>
            <div class="info-card">
                <p style="text-align: center; color: #e74c3c;">
                    <i class="fas fa-exclamation-triangle"></i> Payment information is not available for this order.
                </p>
            </div>
        <% } %>
        
        <h2 class="section-title"><i class="fas fa-history"></i> Order Timeline</h2>
        <div class="timeline">
            <div class="timeline-item">
                <div class="timeline-date"><%= order.createdAt %></div>
                <div class="timeline-event">Order Placed</div>
            </div>
            
            <% if (order.isPaid && order.paidAt) { %>
                <div class="timeline-item">
                    <div class="timeline-date"><%= order.paidAt %></div>
                    <div class="timeline-event">Payment Completed</div>
                </div>
            <% } %>
            
            <% if (order.orderStatus === 'shipped') { %>
                <div class="timeline-item">
                    <div class="timeline-date"><%= order.updatedAt %></div>
                    <div class="timeline-event">Order Shipped</div>
                </div>
            <% } %>
            
            <% if (order.orderStatus === 'delivered') { %>
                <div class="timeline-item">
                    <div class="timeline-date"><%= order.updatedAt %></div>
                    <div class="timeline-event">Order Delivered</div>
                </div>
            <% } %>
            
            <% if (order.orderStatus === 'cancelled') { %>
                <div class="timeline-item">
                    <div class="timeline-date"><%= order.updatedAt %></div>
                    <div class="timeline-event">Order Cancelled</div>
                </div>
            <% } %>
        </div>
        
        <div class="action-buttons">
            <a href="/api/v1/user/profile" class="btn btn-primary"><i class="fas fa-arrow-left"></i> Back to Orders</a>
            <% if (user._id.toString() === order.seller._id.toString() && order.orderStatus === 'placed') { %>
                <a href="/api/v1/orders/update/<%= order._id %>/shipped" class="btn btn-success">
                    <i class="fas fa-truck"></i> Mark as Shipped
                </a>
            <% } %>
            <% if (user._id.toString() === order.buyer._id.toString() && order.orderStatus === 'shipped') { %>
                <a href="/api/v1/orders/update/<%= order._id %>/delivered" class="btn btn-success">
                    <i class="fas fa-check-circle"></i> Mark as Delivered
                </a>
            <% } %>
            <% if (user._id.toString() === order.buyer._id.toString() && !order.isPaid && order.orderStatus === 'pending') { %>
                <form class="cancel-form" action="/api/v1/orders/delete" method="POST">
                    <input type="hidden" name="orderId" value="<%= order._id %>">
                    <button type="submit" class="btn btn-danger"><i class="fas fa-times-circle"></i> Cancel Order</button>
                </form>
            <% } %>
        </div>
    </div>
</div>

<%- include('partials/footer') %>

<!-- Add a confirmation dialog for order cancellation -->
<script>
    document.addEventListener('DOMContentLoaded', function() {
        const cancelForm = document.querySelector('.cancel-form');
        if (cancelForm) {
            cancelForm.addEventListener('submit', function(e) {
                e.preventDefault();
                
                if (confirm('Are you sure you want to cancel this order? This action cannot be undone.')) {
                    // Change the method from POST to DELETE for API compatibility
                    const formData = new FormData(cancelForm);
                    const orderId = formData.get('orderId');
                    
                    fetch(`/api/v1/orders/delete/${orderId}`, {
                        method: 'DELETE',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                        body: JSON.stringify({ orderId }),
                    })
                    .then(response => response.json())
                    .then(data => {
                        if (data.success) {
                            alert('Order cancelled successfully');
                            window.location.href = '/user/orders';
                        } else {
                            alert('Error: ' + (data.message || 'Could not cancel order'));
                        }
                    })
                    .catch(error => {
                        console.error('Error:', error);
                        alert('An error occurred while cancelling the order');
                    });
                }
            });
        }
    });
</script>
</body>
</html>
