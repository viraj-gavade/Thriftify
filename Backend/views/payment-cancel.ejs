<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Cancelled | Thriftify</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .payment-cancel-container {
            max-width: 600px;
            margin: 100px auto;
            padding: 30px;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            text-align: center;
        }
        
        .cancel-icon {
            font-size: 80px;
            color: #e74c3c;
            margin-bottom: 20px;
            animation: shake 0.5s ease-in-out;
        }
        
        .order-status {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin: 25px 0;
        }
        
        .status-badge {
            background-color: #e74c3c;
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-weight: bold;
            display: inline-block;
        }
        
        .action-buttons {
            margin-top: 30px;
        }
        
        .btn {
            padding: 10px 20px;
            margin: 0 10px;
            border-radius: 5px;
            border: none;
            cursor: pointer;
            font-weight: bold;
            transition: all 0.3s;
            text-decoration: none;
            display: inline-block;
        }
        
        .btn-primary {
            background-color: #3498db;
            color: white;
        }
        
        .btn-primary:hover {
            background-color: #2980b9;
        }
        
        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            20%, 60% { transform: translateX(-10px); }
            40%, 80% { transform: translateX(10px); }
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    
    <div class="container">
        <div class="payment-cancel-container">
            <i class="fas fa-times-circle cancel-icon"></i>
            <h1>Payment Cancelled</h1>
            <p>Your payment process was cancelled. No charges were made to your account.</p>
            
            <div class="order-status">
                <h3>Order Status</h3>
                <p><span class="status-badge">Cancelled</span></p>
                <p>Your order has been cancelled. If you'd like to complete your purchase, please try again.</p>
            </div>
            
            <div class="action-buttons">
                <a href="/" class="btn btn-primary">Continue Shopping</a>
                <a href="/user/profile" class="btn">Go to My Profile</a>
            </div>
        </div>
    </div>
    
    <%- include('partials/footer') %>
</body>
</html>
